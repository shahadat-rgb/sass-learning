# sass-learning

  * I have practice Main Feature of SASS.
  * CSS Variable and Nesting csss .
  * Import * Partial .
  * @mixin & @include .
  * @extent & inheritance .
  * @if @else 
  * @for, @while & @each.
