# sass-learning

  * I have practisE Main Feature of SASS.
  * CSS Variable and Nesting csss .
  * Import * Partial .
  * @mixin & @include .
  * @extent & inheritance .
  * @if @else 
  * @for, @while & @each.
